{
  "name": "latino-leaders-2026",
  "version": "1.0.0",
  "description": "Directory of young Latino candidates running for office",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./styles.css --minify",
    "dev": "pnpm build:css && wrangler pages dev . --port 3000",
    "deploy": "pnpm build:css && wrangler pages deploy . --project-name latino-leaders-2026"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "wrangler": "^4.54.0"
  }
}
